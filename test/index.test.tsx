import { createRoot, createSignal } from 'solid-js'
import { describe, expect, it } from 'vitest'
import { DefaultChart } from '../src'

describe('DefaultChart', () => {
    /**
     * TODO:
     * cover the lib with tests
     */
})
